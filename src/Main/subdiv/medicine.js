import React from 'react';


const medicine = (props) =>{

    return(<div>You can use {props.name}  at dosage of {props.dose} , You can make use of {props.parts} to prepare the medicine </div>);

}

export default medicine;